pipelines:
  default:
      - step:
          runs-on:
            - self.hosted
            - linux.shell
          script:
            - pwd
            - ls -la
            - whoami
            - sudo docker ps
          